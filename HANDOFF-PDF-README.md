# Kulibotz Grok Build Handoff PDF

Binary PDF is stored as `Kulibotz-Grok-Build-Handoff.pdf.b64` (base64) for text-safe Git push.

Decode:

```bash
base64 -d Kulibotz-Grok-Build-Handoff.pdf.b64 > Kulibotz-Grok-Build-Handoff.pdf
```

On the crew box: `/workspace/shared/kulibotz-handoff/Kulibotz-Grok-Build-Handoff.pdf`
